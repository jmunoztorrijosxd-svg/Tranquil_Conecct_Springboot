# 1. PUERTO
server.port=8090

# 2. CONEXIÓN A DB (Render usa variables de entorno)
spring.datasource.url=jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}?useSSL=true&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

# 3. JPA/HIBERNATE
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# Eliminado el dialecto explícito, Spring Boot lo detecta automáticamente

# 4. CONFIGURACIÓN DINÁMICA (Esta es la que lee @Value en Java)
BASE_URL=https://tranquil-conecct-springboot.onrender.com

# 5. CONFIGURACIÓN DE CORREO
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=tranquilconnectofficial@gmail.com
spring.mail.password=ngzasspugazwwkxe 
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# 6. LÍMITES
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# 7. SESIONES (Ojo: solo usa 'secure=true' si tienes HTTPS activo en Render)
server.servlet.session.cookie.same-site=Lax